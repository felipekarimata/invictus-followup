# Token de autenticação da Helena CRM
# Gere em: Ajustes > Integrações > Integração via API
HELENA_TOKEN=pn_seu_token_aqui

# Executar imediatamente ao iniciar (para testes)
# true = executa agora | false = só no horário agendado
RUN_NOW=false
